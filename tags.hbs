<ul class="list-inline">
    {{#each tag_cloud}}
       <a class="tag tag-page-tag badge badge-secondary" href="{{ this.url }}" style="font-size: 15px" count="{{ this.count }}">{{ this.tag }} ({{ this.count }})</a>
    {{/each}}
</ul>

<script type="text/javascript">
  $(document).ready(function () {
    $(".tag-page-tag").each(function(i, obj) {
         $(obj).css("font-size", (16 + parseInt($(obj).attr("count"))) + "px");
       });
  });
</script>
